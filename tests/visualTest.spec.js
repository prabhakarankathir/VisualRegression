describe('Verify the working of Visual Regression tests', function () {
    
    it('should load the url', () => {
        browser.url('/');
        console.log('Url loaded successfully');
        browser.pause(2000);
    });
    
     

 
});
